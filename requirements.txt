# Core runtime (Windows/CUDA dev PC and macOS/MPS MacBook both supported).
# On the CUDA dev PC install torch first with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# On the MacBook plain `pip install -r requirements.txt` is enough (MPS wheel is default).
ultralytics>=8.3
opencv-python>=4.10
numpy>=1.26
pyyaml>=6.0
lap>=0.5.12   # ByteTrack association (used by ultralytics model.track)
